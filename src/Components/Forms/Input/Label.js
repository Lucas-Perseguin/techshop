import styled from "styled-components";

export default styled.label`
  font-family: "Poppins";
  font-style: normal;
  font-weight: 900;
  font-size: 16px;

  letter-spacing: 0.05em;

  color: #000000;
`;
